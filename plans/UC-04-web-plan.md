# UC-04 Web - Implementation Plan (Revision 3)

Status: W01/T16 DONE; W02 IMPLEMENTED / LOCALLY VERIFIED; W03 IMPLEMENTED / LOCALLY VERIFIED (legacy auto-recovery removed; Web recovery remains W05); W04 IMPLEMENTED / LOCALLY VERIFIED; W05 IMPLEMENTED / LOCALLY VERIFIED; W06 IMPLEMENTED / LOCALLY VERIFIED; W03-W06 authenticated-navigation integration follow-up IMPLEMENTED / LOCALLY VERIFIED (navbar migrated to in-memory AuthStorage); S01 SESSION RESTORATION IMPLEMENTED / WIRED / LOCALLY + MANUALLY VERIFIED (cookie-backed /auth/web/refresh + useWebSession restoring/authenticated/unauthenticated, driven at runtime by a root-layout WebSessionProvider actually rendered in app/layout.tsx and consumed by the restore-aware navbar and Partner context projection; an earlier import-only wiring claim was revoked after a manual browser F5 failure and corrected with real-composition regression tests; broader S01 coordinator — bounded eligible-401 retry, manual retry joining single-flight, transient handling — NOT EXECUTED); CR-11 PARTNER DIRECT-ROUTE AUTHORIZATION IMPLEMENTED / LOCALLY VERIFIED (shared partnerRouteDecision policy + PartnerRouteGuard reusing S01 state, wrapping /partner, /partner/register, /partner/application and /partner/application/resubmit with a dedicated resubmit key allowing only Rejected; server-side action authorization still required when UC-02/dashboard/resubmit endpoints exist); W07-W08 and remaining session/dependency work NOT EXECUTED. Reference: [spec](../specs/UC-04-web-spec.md).

Read both team rule documents and handoff before audit/code/test changes. Use existing branches; preserve unrelated changes. NO commit/push/new branch/PR/code/DB mutation under this documentation authorization. Core W01/T16 review DONE; runtime implementation requires its own task authorization.

## Core Sign In tasks

| Task | Dependency | Files/areas | Verification / DoD |
| --- | --- | --- | --- |
| W01 documentation-only contract - DONE | Joint FE-BE documentation/read-only audit gate DONE; user sign-off recorded | FE/BE spec + plan edits only; authApi.ts/mapper/controller/DTO read-only references | Complete checklist below; identical reviewed contracts in FE/BE; no runtime changes |
| W02 login context/storage - IMPLEMENTED / LOCALLY VERIFIED | W01, BE Web cookie issuance | proposed src/features/auth/session/, authApi.ts | Memory access, noncredential AuthStorage, cookie persistence selection, validate response/no guesses |
| W03 public/Admin - IMPLEMENTED / LOCALLY VERIFIED | W01,W02 | src/components/auth/SignInForm.tsx, app/sign-in, app/admin/login, tests | Real login, Admin gate, credentials/status precedence, loading/validation/errors; failed B preserves A |
| W04 Google - IMPLEMENTED / LOCALLY VERIFIED | W03 | SignInForm.tsx, feature modal/mapper/tests | Consent/popup/cancel/Partner link/admin denial, BE state authoritative |
| W05 password verification recovery - IMPLEMENTED / LOCALLY VERIFIED | BE Web-only sync,W03 | SignInForm.tsx, authApi.ts/tests | Fresh token, max once, sync no tokens then second login exactly once; sign-in resend matching user |
| W06 role/application routing - IMPLEMENTED / LOCALLY VERIFIED | BE current context,W02 | src/lib/routes.ts, auth routing/guards, proposed app/partner/page.tsx | Pending/rejected/approved from BE; placeholder no fake business; unresolved valid session retained |
| W07 fail-closed | W02, sign-out dependency as available | context validator/session caller/tests | Invalid context no authenticated UI/fallback; attempt cleanup when endpoint available; unavailable revoke BLOCKED, local deletion not success |
| W08 core regression | W03-W07 | relevant FE tests and actual BE integration | Core acceptance evidence, legacy effective status never FE-derived; owning modules excluded |

## Separate session package (retained as requested)

S01: BE refresh + shared FE coordinator/restoration/current context, manual retry joining same single-flight, bounded eligible401 retry/no403 refresh, fixed expiry/nonrotation/transient handling. Relevant authApi/session tests. This package is planned for the delivery, but does not inflate core Sign In. Remembered-session reopen/reload acceptance remains incomplete until implemented.

## Compatibility dependencies / deferred

D01 Web Register: VerifyEmailHandler.tsx/tests migrates to Web-only endpoint, removes saveTokens; full registration/resend waiting page not core. Track compatibility before claiming all Web authentication aligned.
D02 Web sign-out: owning session/sign-out contract, revoked_at+cookie expiry, safe retry; malformed-success cleanup dependency.
D03 multi-tab switching/cleanupPending: deferred, not core mandatory.
D04 DB normalization, approval/reject/resubmit, business authorization module/dashboard: owning UCs; no code/data tasks silently added here. Approved placeholder is core, business module is not.

RED/GREEN/spec review/standards review for meaningful implementation. Focused tests then npm.cmd test/lint/typecheck/build and applicable formatting. BE contract regressions and Mobile compatibility where changed; actual browser/Firebase/DB dependencies reported BLOCKED/SKIP, never mocked PASS. Report separate CORE / SESSION PACKAGE / DEPENDENCY completion, no historical test counts as new acceptance.

### W01 / T16 - Documentation-only contract gate (DONE)

W01 is the FIRST FE-BE agreement step, not a feature implementation. Source files authApi.ts, authErrorMapper.ts, controllers and DTOs are inspection/reference areas only in W01; do NOT modify runtime code/tests, create sessions or change DB. W02 and BE implementation tasks begin only after user approval of the matching final contract.

Approved contract checklist:

- Exact POST routes: /api/v1/auth/web/login, /api/v1/auth/web/admin/login, /api/v1/auth/web/google, /api/v1/auth/web/verify-email. Existing Mobile routes remain unchanged.
- Password/Admin request: application/json with email:string, password:string, keepMeSignedIn:boolean (default false). Example: {"email":"user@example.com","password":"...","keepMeSignedIn":false}. Email trim/lowercase rule; password untouched. Google JSON: idToken:string and keepMeSignedIn:boolean, body-only Firebase token. Verification: Firebase Bearer, no body. Final required/null/unknown-field and validation rules documented before code.
- Sign-in success envelope data types: userId:number (BE integer ID, NOT string); email:string; fullName:string (non-null, empty allowed); role:string enum; status:string effective authentication status; applicationStatus supported string enum for TourOperator (nonoperator always null, field required); accessToken:string; accessTokenExpiresAtUtc:UTC ISO8601 string. Google adds isNewAccount:boolean. Final field nullability and complete success example must agree in both specs.
- Web response has NO refreshToken. Refresh credential only in HttpOnly cookie; internal token issuance may reuse existing BE services without changing the Mobile response.
- Cookie NAME = tripmate_refresh, FINAL/APPROVED; never invent a different name during code. Attributes: HttpOnly, Secure production, SameSite=Lax with HTTPS same-site deployment, Domain omitted, Path=/api/v1/auth. OFF: session cookie; ON: persistent until original seven-day expiry; no rotation/sliding expiry. Local development exceptions and cookie deletion scope documented.
- Legacy resolver BEFORE old legacy gate: DB PendingApproval/profile PendingApproval or DB Rejected/profile Rejected with required role/profile/verification evidence -> effective response status Active, applicationStatus from profile, NO DB account/profile-status update. Conflicts/insufficient evidence -> no session, auth.account_state_unresolved; HTTP403 APPROVED.
- Trusted verification evidence source/provenance established by BE audit. Accepted MVP: DB authoritative; valid persisted EmailVerifiedAtUtc is sufficient evidence; no profile/status inference or response fallback. Keep ordinary password/refresh independent of new implicit Firebase calls.
- Complete endpoint-specific error table: HTTP status + stable code + exact FE display message + action (field error, resend, retry, support, wrong Admin entry). Include validation, credentials, provider/token, account restrictions, Admin Google/Admin role, legacy unresolved, verification and operational/unknown failures. Codes, not message text matching. W01 is NOT DONE while message mappings/catalog remain placeholders.
- Preserve existing Mobile token response/verification session/admin Google guard. Identify current Web Register consumers as compatibility dependency; no implicit client/header-based dispatch or global envelope rewrite.
- Explicit success/error parser contracts: sign-in/Google success envelope, failures ProblemDetails/errorCode; verification follows the APPROVED W01 packet format; Mobile retains existing format. Agree casing/string enums, no role/approval guessing.
- Refresh/sign-out exact schemas are separate session/dependency contract work; do not require full implementation of those UCs in W01. Any field/cookie decision needed by sign-in or cleanup integration must nevertheless be fixed before its consumer code.

Definition of Done: both FE/BE specs contain identical APPROVED route/request/DTO/error/message/cookie/effective-status/evidence decisions; user sign-off recorded 2026-09-14. No code implementation evidence required. User approved the core contract, cookie, evidence, HTTP403 unresolved error and complete error/message/action catalog. W01/T16 = DONE (documentation/audit only); W02/T17+ implementation remains NOT EXECUTED.


## W01/T16 approved solution - DONE

W01 (FE docs) + T16 (BE docs/read-only audit) complete ONE contract gate together; user approval recorded; W02/T17+ prerequisites satisfied (runtime tasks not executed). No T16 runtime implementation prerequisite.

Both specs contain the identical W01/T16 review packet: audited timestamp writers/provenance limitations, APPROVED persisted verification-marker rule, FINAL cookie tripmate_refresh, full HTTP/code/exact-message/action and SDK/fallback catalog. Follow the APPROVED packet; do not invent choices in W02.

Gate status: W01/T16 = DONE, user-approved core contract and code evidence audit. DB authoritative valid timestamps suffice for MVP; suspicious live-data audit is release/migration work. W02/T17+ and other runtime tasks NOT EXECUTED.

DoD includes byte-identical packet in BE/FE specs, no placeholder catalog/cookie, clear source-data trust assumption and compatibility guard; final user approval recorded before implementation. Changes solve earlier vague candidate, unnamed cookie and incomplete mappings without new schema/provider dependency or owning-UC implementation.
## Approved W01/T16 clarification - applicationStatus and evidence

applicationStatus is REQUIRED in EVERY successful Web sign-in and refresh authentication-context response, type string | null; never omitted. Traveler and Administrator -> null. TourOperator with recognized profile -> Approved/PendingApproval/Rejected exactly; missing/unresolved/unsupported profile -> null. BE must serialize this field even if global JSON configuration normally omits nulls. FE validates key presence and type; null for a valid TourOperator triggers application-unresolved UI, not auth cleanup. A missing field is a contract violation, never treated as approved; retain the previously approved valid-session application-unresolved handling rather than signing out solely for application data. Unknown/malformed application status from a nonconforming response remains fail-closed for business access. Verification-only responses are NOT authentication context and do not add applicationStatus. Existing Mobile contract is not silently changed.

Accepted MVP evidence: DB is authoritative; persisted Users.EmailVerifiedAtUtc with CreatedAtUtc <= timestamp <= BE current UTC is sufficient evidence for legacy resolver. NULL/implausible timestamp -> account_state_unresolved. No additional per-record provenance proof or whole-production-DB audit required to implement the resolver. Audit suspicious/imported/manual legacy data before migration/release of those records; do not turn that operational audit into an implementation gate. No data repair/backfill/migration in resolver.

Plan verification: assert applicationStatus key present for Traveler/Administrator (null), each recognized operator status, unresolved operator (null), sign-in/refresh. Assert valid stored evidence passes, NULL/future/pre-creation fails with no session/status mutation. Add serialization coverage to prevent null omission. W01/T16 evidence mechanism and applicationStatus shape are APPROVED by user; Cookie/error-copy decisions are now APPROVED too; this documentation update does not authorize or execute runtime implementation.


## User sign-off record - 2026-09-14

APPROVED: core Web endpoints/request/DTO contract; cookie tripmate_refresh; HTTP403 auth.account_state_unresolved; current error/message/action catalog including Web auth.request_invalid; Web verify-only success ApiResponse and defined failures ProblemDetails/errorCode; fullName non-null string, empty allowed; applicationStatus always present string | null; DB-authoritative valid verification timestamp evidence. W01/T16 = DONE (docs/audit contract gate only). No implementation/test/migration execution by this update. Separate refresh/sign-out schema and deferred owning-UC tasks are not declared DONE. No commit/push/new branch.


## W02 execution evidence - 2026-09-14

- User authorized continuation on feature/PhucTV-sign-in-web, no commit/push/staging/PR/new branch. Existing unrelated FE changes preserved. Earlier documentation-only statements describe the original gate.
- Read both team rules, handoff, FE AGENTS/README/CONTRIBUTING/CODEBASE_RULES and local Next.js browser/server guidance. Approved seams: Web API and session/storage.
- Added browser-only AuthStorage and context parser in src/features/auth/session/authSession.ts. Access/context memory only; optional display metadata in selected localStorage/sessionStorage. Successful sign-in removes only known auth keys from both current-tab stores; no other-tab cleanup claimed. Cached role/context never restores authentication. Expired access unavailable.
- Strict positive safe-integer ID, non-null fullName (empty allowed), supported role, effective Active, usable token/UTC expiry; forbidden Web refreshToken rejected. Exact operator profile values retained; absent/malformed application data becomes null/applicationUnresolved without losing valid auth. No inferred eligibility/approval.
- Added webLogin and webGoogleAuth to authApi.ts: exact Web routes, credentials include, email trim/lowercase, password untouched, default persistence false. HTTP/network failures preserve session A; malformed successful auth context discards memory access. Google isNewAccount checked. No refresh credential exposed or stored by new APIs.
- RED: 15 session behavior failures, 5 API contract failures, then 1 server-boundary failure; logs retained. GREEN final FE suite: 81 passed, 0 failed (24 new W02 tests, baseline 57). Lint/typecheck/build all exit 0; diff whitespace check passed.
- Evidence: ../coverage/UC04-W02/evidence.md and test-output.txt, red-output.txt, red-api-output.txt, red-server-output.txt, lint-output.txt, typecheck-output.txt, build-output.txt, check-exits.txt. Generated evidence ignored by Git.
- Two-pass self-review: spec/state/credential boundaries, then layering/browser scope/error handling/regression. Browser-only guard fixed after RED; no remaining blocker within W02. Human cross-review pending.
- Integration boundary: existing SignInForm and Register consumers still use old API/saveTokens. Their storage behavior is NOT claimed migrated by W02. W03/W04 and D01 must replace those callers; full Web credential-storage acceptance is pending. Refresh/restore S01, server sign-out cleanup D02, multi-tab and live browser/Firebase/SQL acceptance not implemented or counted PASS.
- Next atomic task: W03 public/Admin real form integration. BE T21 waits for FE core completion.


## W03 execution evidence - 2026-09-14

- User approved public/Admin password form migration, persistence checkbox and tests, without staging/commit/push/PR/new branch. Both team rules re-read; existing branch feature/PhucTV-sign-in-web and unrelated dirty changes preserved.
- Modified existing SignInForm.tsx: calls webLogin with normalized email, untouched password and keepMeSignedIn; removed Admin simulation and normal-success saveTokens/Firebase synchronization. Admin redirects to /admin only after successful Web response; Traveler to /. Operator authenticated context retained without home/approval guessing; Partner routing remains W06.
- Updated client/server field validation, disabled/loading guard and password-only error mapper using code + HTTP fallbacks. Raw server text not displayed. Admin errors include public sign-in link; old Open Admin Prototype link removed.
- Tests: baseline 81 passed; RED 5 new form cases failed; final full FE suite 90 passed, 0 failed (9 new cases). Test covers form API arguments, Keep choice, Admin/public success, validation, safe credential/field/account errors, loading and actual API-with-controlled-Response rejection preserving session A. Lint/typecheck/build exit 0, diff whitespace check passed.
- Evidence: ../coverage/UC04-W03/evidence.md, red-output.txt, green-output.txt, test-output.txt, lint-output.txt, typecheck-output.txt, build-output.txt, check-exits.txt. Ignored generated artifacts, no credentials/real provider tokens logged.
- Two-pass self-review: approved task/contract/session boundaries then standards/layers/loading/regression/security. No blocking finding in implemented normal public/Admin password path. Full core acceptance remains pending; human cross-review pending.
- Outstanding user decision: asked whether to remove old public MSG_UNVERIFIED auto-recovery until W05. No answer/authorization yet; old Mobile verifyEmail/saveTokens recovery remains, now only on explicit public MSG_UNVERIFIED (no raw-message inference; never used by Admin). This exceptional path is NOT claimed memory/cookie compliant. W03 is not marked unconditional DONE while decision pending.
- Google and Register old token consumers remain for W04/D01; resend ownership/security and proper fresh-evidence retry remain W05. Refresh/cleanup, Partner routing, real browser/Firebase/SQL integration not implemented or counted PASS. No BE runtime changes or new BE test run.


## W03 approved legacy recovery removal - 2026-09-14

- User continued with "tiep tuc lam" after the concrete proposal to remove old public auto-recovery and keep guidance/resend. Approval applies to this cleanup; W04/W05 runtime work not silently authorized/executed by this change. Earlier pending-decision statements are historical.
- Re-read TEAM_ENGINEERING_RULES.docx and Dev_and_CrossReview_Checklist.pdf, handoff and applicable FE workflow; preserve existing branch/unrelated work.
- Removed only SignInForm public MSG_UNVERIFIED auto-Firebase/reload/Mobile verifyEmail/saveTokens/route-home recovery block and unused verifyEmail import. The explicit unverified message/resend action remains. Password rejection never creates a verification session, auto-retries login or guesses role from Firebase. Manual password submit is still available.
- RED: new regression failed because legacy verified-Firebase path still showed success instead of guidance. GREEN: guidance/no automatic calls now passes even if Firebase mock could report verified. Added explicit resend (no TripMate token issuance) and existing-A memory preservation/manual-submit tests.
- Final FE suite: 93 passed, 0 failed, 10 files (14 SignInForm cases). Lint/typecheck/build exit 0; diff whitespace check passed. Logs in ../coverage/UC04-W03/: red-recovery-output.txt, green-recovery-output.txt, final test-output.txt, lint/typecheck/build outputs and check-exits.txt.
- Two-pass self-review: approved cleanup/response/session boundary, then minimal code/layers/loading/regression. No blocking finding in approved W03 scope; human cross-review pending. W03 IMPLEMENTED / LOCALLY VERIFIED, not full UC acceptance.
- Remaining Google saveTokens in SignInForm belongs to W04. Register legacy consumers D01, full fresh-evidence Web recovery and resend user matching W05, Partner routing W06, cleanup W07/D02, refresh S01 and live browser/Firebase/SQL acceptance remain pending. Explicit resend smoke coverage does not prove all W05 matching/cooldown/recovery requirements.
- No staging/commit/push/PR/new branch/migration/production data change. Next proposal must be presented for user decision before changing W04 code.


## W04 execution evidence - 2026-09-14

- Explicit user approval: migrate Google to webGoogleAuth, remove Web Google saveTokens, consent modal, errors/tests; preserve BE/DB authority, W05 recovery and W06 Partner routing, no staging/commit/push/PR/create/switch branch. Existing feature/PhucTV-sign-in-web and unrelated work preserved.
- Re-read TEAM_ENGINEERING_RULES.docx and Dev_and_CrossReview_Checklist.pdf, FE workflow/approved spec/plan and relevant local Next.js client/server documentation. Test seams: existing SignInForm user interactions and Web API/session boundary.
- Added feature-local GoogleConsentModal using native dialog, cancel/Escape, Terms/Privacy content reused from existing LegalModal, affirmative consent and /partner/register link. English control labels match current Web UI; semantics match approved Google consent flow. Popup runs only on affirmative user action; no checkbox or automatic popup/redirect fallback.
- SignInForm uses webGoogleAuth(Firebase ID token, remember) body-only Web API with credentials include. Removed Google saveTokens/old googleAuth imports/calls. Successful context uses server identity/role/effective status, not Firebase email/display name/claims. Traveler routes home; operator retains session awaiting W06, no home/approval guesses. No BE/account/profile/evidence mutation code or recovery API added.
- Scoped Google mapper implements exact provider/token/unverified/infra/popup copy; account restrictions use approved common password account copy. Silent popup cancellation; blocked popup requires explicit retry. Admin rejection links password sign-in without email auto-fill. Provider evidence/unverified TripMate failures never invoke password recovery or TripMate resend.
- RED: 4 cases failed (existing Admin Google regression migrated to new seam plus 3 consent/API cases). GREEN focused: 29 SignInForm cases passed. Final full FE suite: 108 passed, 0 failed, 10 files (+15 versus 93 baseline). Lint/typecheck/build exit 0; diff whitespace check passed.
- Evidence: ../coverage/UC04-W04/evidence.md, red-output.txt, green-output.txt, test-output.txt, lint-output.txt, typecheck-output.txt, build-output.txt, check-exits.txt. Generated evidence ignored by Git.
- Regression covers consent before popup, Cancel/Escape, legal content/Partner link, Keep choice, body-only exact Web route/cookies through actual FE API with controlled Response, DB response identity in memory, no manual provider/Mobile token persistence, unsupported account/token/provider errors, silent cancellation/popup blocked, existing A preserved after BE denies Google Admin.
- Two-pass self-review: approved contract/role/state/recovery boundaries, then layers/native dialog/state/loading/error/regression. No blocking finding in approved W04 scope; human cross-review pending. Native dialog modal/focus behavior relies on browser; jsdom test shim implements open/close only, not real focus/inert acceptance. No live browser/Firebase/SQL/BE acceptance counted PASS. Firebase SDK configuration unchanged; no manual token storage or persistence configuration introduced.
- W04 IMPLEMENTED / LOCALLY VERIFIED only. D01 Register old googleAuth/saveTokens remains; W05 fresh-evidence Web recovery/resend user matching, W06 Partner routing, W07 cleanup and S01 refresh/restoration remain NOT EXECUTED. No production DB/BE/Mobile changes or new BE tests. Earlier remaining-Google statements in W02/W03 evidence are historical.


## W05 execution evidence - 2026-09-14

- Explicit user-approved scope: Web recovery/fresh-token/resend only, BE status/role authority, no Mobile legacy recovery/saveTokens/automatic activation/unlock, Partner routing W06, no staging/commit/push/PR/branch change. Existing feature/PhucTV-sign-in-web and unrelated dirty edits preserved. Both team rules, handoff and applicable workflow re-read.
- Added authApi.webVerifyEmail: exact Web verify-email POST, fresh Firebase Bearer, no body and credentials omit (no existing Web cookie sent). Success strictly ApiResponse data {emailVerified:true}; token/role-bearing or malformed verify success rejected. Failures use stable ProblemDetails codes. No AuthStorage/session change from verification.
- Added feature-local webRecovery service: initial webLogin; only HTTP403 MSG_UNVERIFIED triggers submitted-credential Firebase authentication; match normalized email before/after reload; getIdToken(true); Web verify-only; full webLogin retry exactly once outside initial catch. No retry on other restrictions, infrastructure/token/evidence failure or second failure. Applies same server-owned AdministratorOnly entry via webLogin flag; no role guessed from Firebase.
- SignInForm calls service, routes only from full final Web context (Traveler/Admin as already implemented; operator retained for W06), no verify-result routing. Firebase not verified -> instructions/resend. Recovery/token/operational messages scoped by code, never SDK/server message matching.
- Resend service reuses matching current Firebase user or authenticates exact submitted account, checks returned email before sending; no verification/session API call. Form rejects edited email mismatch. Success/provider rate limit starts existing 60-second cooldown; transport failure permits explicit retry; no cooldown-expiry acceptance guarantee. No password/token persistence or SDK persistence setting added. Register D01 consumers not migrated.
- RED: API 6 failures, service stub 13 behavior failures, form integration 4 failures after updating interim W03 tests to W05 contract. First service GREEN attempt exposed a missing isApiError export in test mock; partial mock corrected (harness failure is not a product PASS). Final focused GREEN 49 passed across form/recovery/API; additional resend regressions included in final suite.
- Final full FE suite: 131 passed, 0 failed, 12 files (+23 versus W04 baseline108; SignInForm33). Lint/typecheck/build exit0; whitespace diff check passed. Evidence ../coverage/UC04-W05/evidence.md; RED API/service/form logs, green-output.txt, final test-output.txt, lint/typecheck/build and check-exits.txt. green-service-output.txt contains historical failed mock-harness attempt, not final PASS.
- Two-pass self-review: bounded retry/fresh evidence/matching identity/BE state/credential boundaries, then layering/loading/error/cooldown/regression. No blocker in approved W05 scope; human cross-review pending.
- Existing BE T19 sync may transition only PendingEmailVerification on server-verified evidence under its existing approved restrictions; FE never sets Active, unlocks, alters role or repairs legacy data. No BE handler/status mutation changes in W05; no BE test run or production DB access. Controlled FE tests do not prove DB atomicity/session counts.
- W05 IMPLEMENTED / LOCALLY VERIFIED only; full UC04 acceptance not claimed. Browser/Firebase/SQL live integration remains UNVERIFIED. D01 Register handler still uses old Mobile verification/saveTokens until separately approved; Partner routing W06, fail-closed server cleanup W07/D02, refresh/restoration S01 remain NOT EXECUTED. Historical no-auto-recovery W03 statements describe the interim before W05. No branch/delivery/data actions performed.


## W06 execution evidence - 2026-09-14

User accepted W05 and explicitly authorized Partner routing only, on the existing branch with no staging/commit/push/PR/branch change. Both team rules re-read; implement/TDD guidance and repo/handoff/local Next guide consulted. Approved test seams: final password/Google form navigation and Partner route projection.

Shared signInDestination reads only validated final Web BE context: effective Active required; Traveler -> /, Administrator -> /admin, TourOperator Approved -> /partner, PendingApproval/Rejected -> /partner/application. Null/unknown/unresolved application -> no inferred navigation, valid session retained, exact unresolved message. Recovery routes only after final second Web login; Firebase identity and verify-only result never choose a route.

Existing application route previously accepted query-string status and defaulted pending. It now projects accepted in-memory BE context, ignores query-string status, and exposes no prototype profile/business data or lifecycle controls. /partner is an approved minimal placeholder with no business data, shown only for authoritative Approved operator context. Reload without memory requires sign-in; no restoration/refresh/server cleanup implemented. Route snapshot is presentation, not a server authorization guard or ongoing account-state monitor.

RED: four Partner password outcome failures (33 historical form tests passed). Page RED suite could not load because /partner did not exist: recorded as missing-route evidence, NOT seven behavioral failures. GREEN: 37 form tests initially; final focused 49 passed (42 form +7 route tests). Full FE suite 147 passed, 0 failed across 13 files, baseline W05 131. First lint found synchronous effect state update; corrected with hydration-safe external snapshot. Final lint/typecheck/build and whitespace checks recorded in ../coverage/UC04-W06/.

Two-pass self-review: final-context authority/account restrictions/recognized application/no Firebase guesses and no account mutation; then feature layer/shared routing/hydration/scope/compatibility. No blocking finding within W06. No new recovery, token persistence, activation/unlock or cleanup behavior. Existing W05 recovery untouched except final navigation. No BE/DB/Mobile changes or BE test claims.

W06 IMPLEMENTED / LOCALLY VERIFIED, human cross-review pending. Manual unresolved retry uses S01 refresh coordinator when available and remains NOT IMPLEMENTED here; no fake retry added. W07/W08, S01, D01/D02 and live browser/Firebase/BE/DB remain incomplete/unverified, not full UC04 acceptance. Evidence: ../coverage/UC04-W06/evidence.md.

## W03-W06 authenticated-navigation integration follow-up - execution evidence - 2026-09-14/15

User approved this scoped fix as a W03-W06 integration follow-up, explicitly NOT W09, on the existing feature/PhucTV-sign-in-web branch with no staging/commit/push/PR/branch change. Both team rules re-read; a read-only navigation audit established root cause before any change.

Root cause: PublicNavigation consumed two legacy Web auth sources — localStorage 'tripmate_access_token'/'tripmate_user' and Firebase onAuthStateChanged. Approved W03 cleanup stopped writing both on Web password/Google login, so after a 200 login and client-side navigation the navbar still rendered the unauthenticated "Sign In" state despite a valid in-memory AuthStorage session. This was a presentation-consumer migration gap (sibling of W03-W06/D01), not a new auth capability.

Change: migrated PublicNavigation to the in-memory AuthStorage context as the single client-side source of truth via useSyncExternalStore (established W06 pattern, but with a real subscription). Added an additive AuthStorage.subscribe(listener) notification mechanism, required by approved TDD case 3 (context change must update the navbar without a full reload; the PartnerContextPage no-op-subscribe pattern cannot satisfy it). displayName derives from context fullName falling back to email. Sign Out clears the client session only and is honestly labeled — no server revoke/cookie-clear claim (D02). No saveTokens, no access-token persistence, no Firebase auth-state dependency for Web session truth, no Mobile auth change, no refresh/reopen restoration (S01), no BE role/status change, no /sign-in guard, no server-side logout work. W03-W06 Traveler/Admin/Partner routing preserved.

TDD: RED recorded four PublicNavigation failures against the legacy implementation plus one authSession failure (subscribe missing), all for the right reasons. GREEN focused 23 passed (4 navbar + 19 session). Interim GREEN fixes: subscribe must forward React's onStoreChange callback (an initial draft registered a no-op), and accept() must notify exactly once (it previously called clear() internally causing a double notify; the storage cleanup was inlined). Full FE suite 152 passed, 0 failed across 14 files, baseline W06 147/13 plus 5 new. First lint flagged one unused-import warning; removed and lint rerun. Final lint/typecheck/build exit 0. Evidence in ../coverage/UC04-W03-W06-integration/.

Self-review: presentation-only consumer, feature-layer session module owns notification, SSR-safe serverSnapshot, no new credential storage writes; existing 147 baseline tests unchanged (accept() storage semantics preserved). AC09 memory-access/correct-UI now holds for same-tab client navigation; backend-authoritative reload restoration remains S01 and is NOT claimed. No blocker within approved scope.

Status: IMPLEMENTED / LOCALLY VERIFIED, human cross-review pending. F5/reopen still shows Sign In until S01 restoration; Sign Out does not revoke the server session or clear the HttpOnly refresh cookie (D02); the navbar is presentation, not an authorization guard. Live browser/BE/Firebase/DB remain unverified. Evidence: ../coverage/UC04-W03-W06-integration/evidence.md.

### Partner-item navigation decision (same integration round) - 2026-09-15

Manual follow-up "clicking Partner shows Sign In again" was audited read-only first: not a reload and not session loss. /partner/register renders its own static header (PartnerShell) with a hardcoded "Sign In" link that never reads AuthStorage — a separate presentation consumer, classified as route/layout remount to an auth-unaware header. AuthStorage context remains intact there; it is merely ignored.

Authoritative navigation decision (supersedes an earlier interim CTA proposal): the "Partner" entry is shown ONLY to Guests (the future UC-02 "Register Tour Operator Account" entry point) and TourOperators. An authenticated Traveler sees NO Partner item and NO operator-registration CTA; /partner/register is not exposed from PublicNavigation for Travelers. An authenticated Administrator sees no Partner/registration entry (Admin navigation preserved separately). TourOperator contexts route the Partner item through the existing shared signInDestination projection (Approved -> /partner; PendingApproval/Rejected -> /partner/application; unresolved -> /partner/application, W06 fail-closed preserved, no invented state). Guest keeps generic Partner -> /partner/register. NOTE: an interim revision added a "Become a Tour Operator" CTA for Travelers; that was incorrect and has been removed, with tests updated.

Preserved: guest access to /partner/register, operator application/dashboard mapping, no account linking/upgrade/merge/role mutation/Traveler-identity reuse, no S01/D02 expansion, no UC-02 backend, no dashboard, no resubmit/rejection handling, no ?status= fix, no CR-11 route authorization, no PartnerShell change.

TDD: RED 2 focused failures (Traveler sees no Partner/CTA; Administrator sees no Partner registration entry) against the interim CTA build, other 9 cases passing unchanged; GREEN focused 11/11; full FE suite 159 passed, 0 failed across 14 files; lint/typecheck/build exit 0. Evidence in ../coverage/UC04-W03-W06-integration/ (red-nav-final-output.txt, green-nav-final-output.txt, test/lint/typecheck/build/check-exits).

Open item reported, NOT changed: PartnerShell's static "Sign In" header on /partner/register for authenticated visitors is a separate presentation decision awaiting product direction; no shell migration was performed in this round.

## S01 minimal session restoration - execution evidence - 2026-09-15

User approved S01 scoped minimally to unblock CR-11 direct-route authorization, explicitly NOT CR-11 guards, NOT D02, NOT UC-02, NOT dashboard/resubmit UI, NOT auth redesign. Both team rules re-read; the preceding CR-11 audit established the blocker before any change.

Why: the CR-11 audit proved identity was unrecoverable after hard navigation. The HttpOnly tripmate_refresh cookie was written on login but never consumed anywhere (no /refresh, no /me, no cookie reader; grep of Request.Cookies returned only the constant declaration), and in-memory AuthStorage resets on F5. A client guard keyed on missing context would fake authorization by treating a restored-but-logged-in user as Guest. S01 closes exactly that gap so CR-11 can evaluate authoritative identity after restore.

BE: added POST /api/v1/auth/web/refresh (AuthController.cs) reading only the tripmate_refresh cookie (no body). WebRefreshCommandHandler hashes the raw token via IJwtTokenService.HashRefreshToken, looks up dbo.RefreshTokens AsNoTracking by token_hash, and rejects missing/revoked/expired rows with AUTH_TOKEN_INVALID (401 via HandleFailure). It resolves the user and re-runs the SAME AccountEligibilityResolver used by login, so account gates (Locked/Inactive/PendingEmailVerification) and the legacy PendingApproval/Rejected-marker rules apply unchanged, and application status is re-read from the CURRENT OperatorProfiles row at restore time (approval changes after login are reflected). It returns the identical WebAuthResponseDto projection as login (userId, email, fullName, role, effective status, current applicationStatus, fresh access token). Non-rotating by contract: the refresh row is never modified, no Set-Cookie is emitted, and the raw credential is never serialized. No logout/revoke behavior (D02 stays separate).

FE: webRefresh() in authApi.ts (POST credentials:'include', no body; a network failure is a recoverable NETWORK error that never clears an existing session; a definitive 401/403 clears memory and leaves the user genuinely unauthenticated; a malformed success is discarded without authenticating; a valid success flows through the existing strict AuthStorage.accept validator). New useWebSession() hook exposes explicit status 'restoring' | 'authenticated' | 'unauthenticated': a missing in-memory context is NOT Guest until the cookie-backed restore settles; single-flight per document; skips the call when memory already holds a context; subscribes to AuthStorage so late updates propagate; a session warm at mount seeds the settled flag so a later Sign Out settles to unauthenticated instead of hanging in restoring.

FE integration (runtime wiring, this round): the restore is wired into the real runtime at the narrowest shared point. WebSessionProvider ('use client', src/features/auth/session/WebSessionProvider.tsx) is mounted once in the root layout (app/layout.tsx) so every Web route — including /partner and /admin, which do not render the navbar — drives the single-flight cookie restore on cold start before any role/status-sensitive UI decides Guest. The provider only runs the restore; it does NOT gate or redirect (that is CR-11). PublicNavigation now consumes useWebSession() instead of a raw AuthStorage snapshot: it renders an accessible restoring placeholder (role='status') while the cookie restore is pending, so it never flashes Sign In / guest Partner+Register for a session about to be restored, and shows authenticated or unauthenticated controls only after settle. This is restoration wiring, not authorization: no route guard, no CR-11 matrix enforcement was added.

**SUPERSEDED by manual runtime finding (see "Runtime wiring correction" below):** the paragraph above stated the provider was mounted in the root layout, but the real browser showed app/layout.tsx only imported it without rendering it, and PartnerContextPage used a no-op subscription — so a hard refresh on /partner/application issued zero requests and falsely rendered "Please sign in". The "WIRED" claim was not true at runtime and is corrected below.

Invariants preserved: current login behavior unchanged; no access token in localStorage/sessionStorage; persisted tripmate_user metadata never authenticates (explicit test); the cookie remains the only restore credential; no role/status/applicationStatus inferred from client state, email, Firebase, or metadata.

Incidental blocker found and cleared: RegisterTravelerCommand.cs was corrupt in the BE working tree (declared ConfirmPassword/GoogleToken/Result<AuthResponseDto>, contradicting its own validator (PhoneNumber), handler (FirebaseIdToken) and response type (RegisterTravelerResponse)); not an S01 file. It broke compilation of the whole Application project. Restored that single file to its committed HEAD version (git checkout HEAD -- <file>); no commit/stage/push/branch change. Flagged for human review to confirm the corruption source.

TDD: [RED](../coverage/UC04-S01/red-output.txt) — BE compile failure (handler absent), integration 404 (endpoint absent), FE webRefresh is not a function + unresolvable hook import, all for the right reasons. GREEN: BE unit 18/18 (valid-session no-rotation; Traveler/Administrator/TourOperator Approved/PendingApproval/Rejected/null/unknown-enum application context; missing/empty/unknown token; expired; revoked; Locked/Inactive/PendingEmailVerification gates; legacy marker parity) + BE integration 8/8 (login cookie redeems context with NO new Set-Cookie; three operator statuses; approval-change-after-login; no-cookie 401; unknown-cookie 401; revoked-cookie 401) + FE focused 8/8 (restore shape/credentials/no-body; operator applicationStatus preserved; 401 leaves unauthenticated; malformed discards; restoring-before-authenticated ordering; memory-hit skips endpoint; persisted metadata alone never authenticates; hard-navigation simulation restores before routing).

Integration TDD (this round): [RED](../coverage/UC04-S01/red-integration-output.txt) — 8 focused failures proving the navbar/hook were not yet restore-aware (guest Partner/Sign In shown before settle, no restoring placeholder, warm Sign Out stuck, restored roles not applied). [GREEN](../coverage/UC04-S01/green-integration-output.txt) — session+navigation 56/56 (PublicNavigation restore-aware 20, useWebSession 5, WebSessionProvider 2, webRefresh 4, authSession 19).

Full validation: BE suite 364 passed / 0 failed / 8 skipped (baseline 338 + 26 new), build 0 errors (BE unchanged this round). FE tests 179 passed / 0 failed / 17 files (baseline 167 + 12 net new). FE lint/typecheck/build exit 0. Lint iterations: a synchronous setState-in-effect pattern (hook) was corrected to a lazy settled-state initializer; a ref-during-render error was replaced with useState; final lint clean. Evidence in ../coverage/UC04-S01/.

Status: S01 SESSION RESTORATION IMPLEMENTED / WIRED / LOCALLY VERIFIED, human cross-review pending. The cookie-backed restore now runs in the real runtime (root-layout provider + restore-aware navbar), so a hard refresh/direct navigation repopulates AuthStorage before role/status-sensitive UI decides Guest. Broader S01 coordinator (bounded eligible-401 retry, manual retry joining the same single-flight, transient handling) and CR-11 route guards remain NOT EXECUTED — CR-11 is the next task and now has a verified restoration mechanism to build on. Server-side authorization is still required for any real action endpoint (UC-02 registration does not exist; AdminTourOperatorApplicationsController is already [Authorize(Roles=Administrator)]). Cookie is non-rotating with fixed 7-day expiry per the approved W01 contract; revocation/logout remains D02. Live browser/SQL round-trip remains unverified.

## S01 runtime wiring correction - execution evidence - 2026-09-15

Manual browser verification (TourOperator PendingApproval login -> /partner/application -> F5) contradicted the previous WIRED claim: Network showed ZERO requests, no POST /auth/web/refresh, and the page immediately rendered "Please sign in to view your Partner context." A read-only audit found the exact runtime defects: (1) app/layout.tsx imported WebSessionProvider but never rendered it — the provider was dead code in the real composition, and the eslint unused-import warning on that file was the missed signal (the lint script does not fail on warnings); (2) PartnerContextPage consumed AuthStorage via a no-op useSyncExternalStore subscription and evaluated null context as unauthenticated on first paint, so it could neither wait for nor observe a restore. The hook, provider component, webRefresh client and BE endpoint were correct in isolation — the automated suites mounted the provider manually or self-restored via the navbar, so they never exercised the real layout composition for navbar-less routes. The previous "WIRED" classification is revoked and replaced by this correction.

Smallest correct fix (approved scope, no new restoration mechanism): app/layout.tsx now actually renders <WebSessionProvider>{children}</WebSessionProvider> inside body — provider mounted exactly once at the shared root, existing styles/fonts/metadata untouched, no authorization logic. PartnerContextPage now consumes useWebSession(): 'restoring' renders a neutral accessible placeholder (role='status'); 'unauthenticated' renders the existing sign-in content; 'authenticated' preserves the W06 projection exactly (Approved -> approved area, PendingApproval/Rejected -> application status, null -> fail-closed unresolved message). No inference, no metadata/Firebase/email source, no second restore mechanism.

TDD with REAL cold-start composition: new src/features/auth/routing/PartnerContextPage.runtime.test.tsx renders the ACTUAL app/partner pages and the ACTUAL app/layout module. [RED](../coverage/UC04-S01/red-wiring-output.txt): 5/6 failed reproducing the browser bug exactly — cold /partner/application showed "Please sign in" before settle (A), cold /partner same (B), unresolved restore never rendered (C), 401 settle ordering (D), and the layout-composition guard F proved webRefresh was called ZERO times through the real RootLayout (maintainable anti-regression check; no brittle text-only assertion). Warm-memory E passed as expected. [GREEN](../coverage/UC04-S01/green-wiring-output.txt): auth+navigation 88/88 across 10 files including all six runtime cases.

Full validation: FE npm test **185 passed / 0 failed / 18 files** (179 + 6 runtime regression), exit 0. Lint exit 0 with the WebSessionProvider unused-import warning eliminated (layout.tsx verified individually clean). Typecheck exit 0. Build exit 0. BE unchanged (364 passed / 0 failed / 8 skipped stands).

Premature CR-11 artifacts: two CR-11 matrix test files drafted before this correction were set aside (coverage/UC04-CR11-pending/*.pending, not executed) because CR-11 is explicitly NOT part of this task. No authorization guard, redirect matrix, or route protection was implemented.

Classification: S01 mechanism + runtime wiring now IMPLEMENTED / LOCALLY VERIFIED with a real-composition regression suite; final DONE reclassification requires the user's manual browser verification (F5 on /partner/application must show POST /auth/web/refresh 200, no "Please sign in" flash, PendingApproval after settle; plus Traveler/Administrator/Approved/Rejected/unresolved F5 restores). Evidence in ../coverage/UC04-S01/ (red-wiring-output.txt, green-wiring-output.txt, s01-* refreshed).

## CR-11 Partner direct-route authorization - execution evidence - 2026-09-15

Approved after S01 restoration was accepted DONE / LOCALLY + MANUALLY VERIFIED. Scope: client-side route authorization for the three Partner Web routes only; no S01 redesign, D02, UC-02 backend, dashboard, resubmit/rejection features, or new endpoints.

Mechanism (one shared policy + one reusable guard, reusing S01 state): partnerRouteDecision.ts is a pure (route, authoritative context) -> allow | redirect policy evaluated only after the session settles — a null context there means genuinely unauthenticated, never "restoring". PartnerRouteGuard.tsx consumes useWebSession(): while restoring it renders a neutral accessible placeholder (no Guest form, no protected content, no wrong-role message, no redirect); once settled, allow renders children unchanged and redirect issues router.replace (unauthorized URL never committed). All decisions read only server-provided role/effective status/applicationStatus; no email/Firebase/persisted-metadata/URL inference.

Approved matrix implemented: /partner/register — guest ALLOW (future UC-02 entry), Traveler DENY -> /, Administrator DENY -> /admin, operator Approved -> /partner, PendingApproval/Rejected/unresolved -> /partner/application (fail-closed preserved). /partner — ALLOW only Active TourOperator Approved; other operator states -> /partner/application; Traveler -> /, Administrator -> /admin, guest -> /sign-in. /partner/application — PendingApproval/Rejected/unresolved ALLOW (unresolved keeps the W06 fail-closed message); Approved -> /partner; Traveler/Administrator/guest as above. Wrong-role authenticated users are never shown "Please sign in"; they land on their own safe destination.

Routes wrapped for direct-URL entry: app/partner/page.tsx (dashboard), app/partner/application/page.tsx (application), app/partner/register/page.tsx (register). Hiding navbar links remains presentation; this guard is the navigation-level enforcement.

TDD: [RED](../coverage/UC04-CR11/red-output.txt) — matrix suites restored from the set-aside pending files failed to resolve ./partnerRouteDecision and ./PartnerRouteGuard (sources absent). [GREEN](../coverage/UC04-CR11/green-output.txt) — auth suite 103/103. Coverage A-H + warm-memory + hard-navigation simulations (empty memory + mocked cookie restore: restored Traveler denied on register, Approved allowed on dashboard, PendingApproval redirected). Two S01 runtime tests were corrected to the approved matrix (settled guest on the application route now redirects to /sign-in instead of rendering legacy body) and the real /partner/register page gained two cold-start composition cases; historical S01 evidence files remain unchanged.

Full validation: FE npm test 220 passed / 0 failed / 20 files (baseline 185 + 35: decision 21, guard 12, register composition 2). Lint exit 0 (0 warnings), typecheck exit 0, build exit 0. BE unchanged. Evidence in ../coverage/UC04-CR11/.

Server-side gaps (reported, NOT implemented): UC-02 registration / dashboard / resubmit action endpoints do not exist yet, so there is nothing further to enforce server-side today; existing BE actions keep their role-aware [Authorize] + handler re-checks. When those endpoints are implemented, server-side authorization from the access token is mandatory — this client guard is navigation protection, not a security boundary.

Status: CR-11 PARTNER DIRECT-ROUTE AUTHORIZATION IMPLEMENTED / LOCALLY VERIFIED, human cross-review pending; manual browser verification (typed-URL denials + F5 restore-then-authorize) required before DONE reclassification.

### CR-11 resubmit route + clearTokens cleanup - 2026-09-15

The post-CR-11 cleanup audit found /partner/application/resubmit reachable by direct URL without a guard. Approved correction: resubmission is its own permission and must NOT inherit the application screen's broader access. partnerRouteDecision gained a dedicated 'resubmit' route key: Guest -> /sign-in, Traveler -> /, Administrator -> /admin, Approved -> /partner, PendingApproval -> /partner/application (still under review — explicitly must not gain Rejected's resubmit right), Rejected -> ALLOW, unresolved/null -> /partner/application fail-closed with no inferred Rejected. app/partner/application/resubmit/page.tsx wraps ResubmitApplicationForm in <PartnerRouteGuard route="resubmit">; the form's mock logic and simulateMockRequest are untouched and no BE resubmit endpoint was added.

TDD: [RED](../coverage/UC04-CR11/red-resubmit-output.txt) — 4 decision + 7 guard failures (guest/Traveler/Administrator cases passed early via shared returns, correctly retained). One RED failure was a test-authoring mistake (deny-pattern assertion left on the Rejected ALLOW case); fixed in the test, not the source. [GREEN](../coverage/UC04-CR11/green-resubmit-output.txt) — routing 65/65 (decision 28, guard 22; includes restoring-no-form and both hard-navigation restore-first scenarios).

Cleanup executed per approval: removed ONLY the dead clearTokens() export from src/lib/authApi.ts (zero references app-wide including tests). OperatorApplicationStatusPage, login()/LoginRequest/LoginResponse, RefreshToken.IsActive, src/legacy/mobile/**, and all evidence/spec/plan files retained per instruction.

Full validation: FE npm test 237 passed / 0 failed / 20 files (220 + 17 resubmit), lint exit 0 (0 warnings), typecheck exit 0, build exit 0. BE unchanged.
