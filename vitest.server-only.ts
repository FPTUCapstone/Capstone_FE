// Vitest stub for the `server-only` package (a build-time guard with no runtime
// behavior). The alias in vitest.config.mts points here so server modules can be
// unit-tested in the jsdom/node environment.
export {};
