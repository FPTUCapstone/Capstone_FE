/**
 * Navigation states from the original Vite visual prototype.
 * They are retained only so the legacy mobile-reference components remain compilable.
 */
export type AppView =
  | 'landing'
  | 'traveler-home'
  | 'plan-trip'
  | 'suggested-itinerary'
  | 'live-nav'
  | 'reroute-proposal'
  | 'operator-dash'
  | 'admin-dash'
  | 'admin-tour-review';
